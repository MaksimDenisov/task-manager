app:
  scheduler:
    report-cron: "*/10 * * * * *"
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://db:5432/tm_db
    username: user
    password: password
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
